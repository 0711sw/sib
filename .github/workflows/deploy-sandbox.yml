name: Deploy Sandbox

on:
  push:
    branches:
      - develop

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    name: Deploy to Sandbox
    uses: ./.github/workflows/deploy-descriptors.yml
    with:
      target: t01.durablox.net
